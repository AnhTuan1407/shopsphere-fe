type ProductVariants = {
    id?: number,
    color?: string,
    size?: string,
    version?: string,
    imageUrl?: string,
    price?: number,
    availableQuantity?: number,
    rating?: number,
    quantitySold?: number,
}

export default ProductVariants;