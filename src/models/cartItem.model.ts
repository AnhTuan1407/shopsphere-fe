
type CartItem = {
    id?: number,
    price?: number,
    productId?: number,
    productVariantId?: number,
    supplierId?: number,
    quantity?: number,
    selected?: boolean,
}

export default CartItem;
