type Supplier = {
    id: number,
    name?: string,
    rating?: number,
    imageUrl?: string,
    address?: string,
    phoneNumber?: string,
    contactEmail?: string,
    followers?: number,
    createdAt?: string,
}

export default Supplier;