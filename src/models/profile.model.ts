type Profile = {
    id?: string,
    firstName?: string,
    lastName?: string,
    username?: string,
    password?: string,
    email?: string,
    phoneNumber?: string,
    userId?:string,
}

export default Profile;
